---
  name: Validate-YAML

  on:
    pull_request:
      branches: [main]

  jobs:
    validate-yaml:
    runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v3
        - name: Show yamllint version
          run: yamllint --version
        - name: Run lint
          run: yamllint .
